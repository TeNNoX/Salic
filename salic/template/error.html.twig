<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>SaLiC Error</title>
</head>
<body>

<div>
    <h1>I'm sorry, but I had a problem while trying to render the page :/</h1>

    If you are not the Webmaster,<br>
    <a href="/" style="font-size: 1.5em;"> &gt; return to the homepage &lt;</a>. <br>
    Please come back later.

    <br><br><br>
    <br><br><br>
    <br><br><br>
    <hr>

    <h3>Webmaster info:</h3>

    <b>Happened while</b>: {{ while }}<br>

    <b>Exception</b>: {{ exception|get_class }} <br>
    <b>Message</b>: {{ exception.getMessage() }} <br>
    {% if exception.fileInfo is defined and exception.fileInfo is not null %}
        <b>FileInfo</b>: {{ exception.fileInfo }} <br>
    {% endif %}

    <br>
    Trace:
    <pre>{{ exception.getTraceAsString() }}</pre>

    {% if exception.data is defined and exception.data is not null %}
        <br>
        Data:
        <pre>{{ exception.data|json_encode(constant('JSON_PRETTY_PRINT')) }}</pre>
    {% endif %}
</div>
</body>
</html>